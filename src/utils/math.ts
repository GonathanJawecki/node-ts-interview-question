export const getMean = (input: any): any => {
  return 0;
};

export const getMedian = (input: any): any => {
  return 0;
};

export const getMode = (input: any): any => {
  return [0];
};

export const getStandardDeviation = (input: any): any => {
  return 0;
};
